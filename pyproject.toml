[project]
name = "crispy-octo-robot"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "Dex", email = "dex@gmail.com"},
]
dependencies = [
    "requests",
    "beautifulsoup4",
    "fastapi",
    "uvicorn",
    "python-multipart",
    "jinja2",
    "sentence-transformers",
    "scikit-learn",
    "pytest",
    "playwright"
]
requires-python = ">=3.11"
readme = "README.md"
license = {text = "MIT"}

[tool.pdm]
distribution = false
